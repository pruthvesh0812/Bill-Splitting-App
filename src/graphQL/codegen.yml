overwrite: true
schema: "./schema.gql"
generates:
  ./graphql-types.ts:
    plugins:
      - typescript
      - typescript-resolvers
    config:
        contextType:"./context#DataSourceContext"
     